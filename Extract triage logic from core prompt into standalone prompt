Currently the artefact triage logic is embedded in the core prompt.

Goal: Extract into a separate, independently testable triage prompt.

Benefits:
- Modular — triage can be updated without touching analysis logic
- Testable in isolation
- Transferable to aXIOM independently via PROMPT_GOVERNANCE.md pipeline

Status: backlog — no active development until core prompt is stable.

Related: PROMPT_GOVERNANCE.md
